=persist() 시작=
Hibernate: 
    /* insert hellojpa.Member
        */ insert 
        into
            Member
            (id, name, roleType) 
        values
            (null, ?, ?)
member.getId() = 1
=persist() 끝=
=commit() 시작=
=commit() 끝=


<정리>
IDENTITY 버전의 경우 db에 id 생성을 위임한다.
영속성 컨텍스트에 값을 저장하기 위해서는 pk가 있어야 하기 때문에
IDENTITY 버전의 경우는 EntityManager.persist() 시점에 쿼리를 날려서
db가 id값을 생성하도록 한다.
